function Recommendations() {
  // i want to download top 5 genres, then i have to download them from discogs on scroll for the covers and ids
  // on scroll it will download more albums from each genre
  // if all albums are exhausted, download more genres?

  return <div></div>;
}

export default Recommendations;
